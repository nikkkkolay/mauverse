import { FormSection, AboutSection } from '../../sections';

export const Main = (): JSX.Element => {
    return (
        <>
            <AboutSection />
            <FormSection />
        </>
    );
};
