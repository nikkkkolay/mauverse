import { ContactSection, AboutSection } from '../../sections';

export const Main = (): JSX.Element => {
    return (
        <>
            <AboutSection />
            <ContactSection />
        </>
    );
};
