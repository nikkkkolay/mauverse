import { AboutSection, NewsSection } from '../../sections';

export const MainPage = (): JSX.Element => {
    return (
        <>
            <AboutSection />
            <NewsSection />
        </>
    );
};
